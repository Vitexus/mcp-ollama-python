#!/usr/bin/make -f
export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# Skip tests during build - they require typing_inspection transitive dependency
	# Tests can be run separately with: python3 -m pytest tests/
